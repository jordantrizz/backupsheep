# Next Steps

- Testing: run the `lite`, `standard`, and `full` startup validation paths and capture any remaining self-hosted compatibility gaps.
- Git commit: keep the compatibility and bootstrap work split into focused commits as follow-up fixes land.
